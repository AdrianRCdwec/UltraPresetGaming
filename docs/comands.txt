--------------------------------------------------------------
INICIO DEL PROYECTO EN UN NUEVO DISPOSITIVO:
--------------------------------------------------------------
    git pull
    cd .\backend\
    py -m venv .venv
    .venv\Scripts\activate
    pip install -r dependencies.txt
    playwright install chromium



--------------------------------------------------------------
ACTUALIZAR EL PROYECTO AL DÍA:
--------------------------------------------------------------
    git pull
    cd .\backend\
    .venv\Scripts\activate
    pip install -r dependencies.txt


--------------------------------------------------------------
RESET DE LA BASE DE DATOS:
--------------------------------------------------------------
    del db.sqlite3
    python manage.py migrate
    python manage.py createsuperuser



--------------------------------------------------------------
COMANDOS DENTRO DEL ENTORNO:    ( .venv\Scripts\activate )
--------------------------------------------------------------
    EJECUTAR CRAWLER (DESDE BACKEND) (ACTUALIZAR/AÑADIR PRODUCTOS):
        python -m scrapper_app.main_crawler

    ENCENDER SERVIDOR:
        python manage.py runserver

    CREAR/ACTUALIZAR ARCHIVO DEPENDENCIES.TXT:
        pip freeze > dependencies.txt

    INSTALAR TODAS LAS DEPENDENCIAS NECESARIAS:
        pip install -r dependencies.txt

    VACIAR DATOS SIN BORRAR LA ESTRUCTURA:
        python manage.py flush

    ACTUALIZAR DE FORMA INTERACTIVA (PREGUNTA EN CADA DEPENDENCIA)
        pip-review --interactive

    ACTUALIZAR TODAS LAS DEPENDENCIAS:
        pip-review --auto

--------------------------------------------------------------
COMANDOS IMPORTANTES:
--------------------------------------------------------------
    DESHACER UN COMMIT PUSHEADO (EL NÚMERO ES EL NÚMERO DE COMMITS A DESHACER)
        git reset HEAD~1

    DESHACER UN COMMIT NO PUSHEADO (EL NÚMERO ES EL NÚMERO DE COMMITS A DESHACER)
        git reset --soft HEAD~1
    
    GENERAR EL ÁRBOL
        tr
    
    GENERAR EL ÁRBOL SIN ARCHIVOS
        tr -f